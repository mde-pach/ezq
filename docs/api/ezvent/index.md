# `ezvent` API

The `ezvent` package provides the main interface for working with events.

## Core Components

::: ezvent
    options:
        members:
            - on_event
            - publish_event
            - publish_events
            - EZvent
            - EZEndEvent
            - EZInterruptEvent
        show_root_heading: true
        show_source: true
