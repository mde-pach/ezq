# `ezq` API

The `ezq` package provides the queue implementation used by `ezvent`.

## Core Components

::: ezq
    options:
        members:
            - consumer
            - get_queue
            - DEFAULT_QUEUE_NAME
            - configure
            - get_config
            - EZQConfig
        show_root_heading: true
        show_source: true
